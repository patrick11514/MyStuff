{
    "name": "create_patrick115_app",
    "version": "1.0.0",
    "description": "",
    "main": "./build/index.js",
    "scripts": {
        "dev": "ts-node-dev --respawn ./src/index.ts",
        "build": "mkdir -p build && tsc -p tsconfig.json ",
        "start": "node ./build/index.js",
        "clear": "rm -r build",
        "depl": "pnpm run build && node ./deploy.js && cp build/* ~/scripts && cp CreatePatrick115App ~/scripts && cd ~/scripts && pnpm i && chmod +x CreatePatrick115App"
    },
    "keywords": [],
    "author": "Patrik Mintěl",
    "license": "ISC",
    "dependencies": {
        "cli-color": "^2.0.3",
        "enquirer": "^2.4.1",
        "node-fetch": "^3.3.2"
    },
    "devDependencies": {
        "@types/cli-color": "^2.0.3",
        "@types/inquirer": "^9.0.3",
        "@types/node": "^20.8.2",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.2.2"
    }
}
